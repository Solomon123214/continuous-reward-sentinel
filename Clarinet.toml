[project]
name = "continuous-reward-sentinel"
authors = ["Blockchain Innovations Lab"]
description = "A decentralized reward tracking system for monitoring and incentivizing continuous achievements"
telemetry = false
requirements = []
[contracts.reward-sentinel]
path = "contracts/reward-sentinel.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
